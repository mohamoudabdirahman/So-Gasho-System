import 'package:flutter/material.dart';

class Sites extends StatelessWidget {
  const Sites({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sites', style: TextStyle(fontSize: 30),),
    );
  }
}